if [ -f "/etc/apt/apt.conf.d/02proxy" ]; then rm /etc/apt/apt.conf.d/02proxy; fi
rm /home/vagrant/.ssh/authorized_keys
#usermod -p $(perl -e 'print crypt("changeme", "MB")') vagrant
#groupadd developer
#usermod -d /home/developer -m -g developer -l developer vagrant
